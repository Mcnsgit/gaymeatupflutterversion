import 'package:flutter/material.dart';

class VideoChatsPage extends StatelessWidget {
  const VideoChatsPage({Key? key}) : super(key: key);
  
  @override
  Widget build(Object context) => throw UnimplementedError();
  // The same VideoChatsPage from before goes here.
}
