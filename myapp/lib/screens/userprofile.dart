import 'package:flutter/material.dart' show StatelessWidget;

abstract class ProfilePage extends StatelessWidget {
  const ProfilePage({super.key});
  // The same ProfilePage from before goes here.
}
